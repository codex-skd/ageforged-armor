<h2>v0.0.0-beta.28 — Structural refactor + workflow alignment v1.0.0</h2>

<h3>Refactor</h3>
<ul>
<li><strong>Class rename: entry point</strong> — <code>ArmorOfTheAgesForge</code> renamed to <code>AgeforgedArmor</code> to match mod_id naming convention and remove legacy &quot;Forge&quot; + original mod name remnants.</li>
<li><strong>Class rename: client</strong> — <code>ArmorOfTheAgesClientForge</code> renamed to <code>AgeforgedArmorClient</code>.</li>
<li><strong>Class rename: loot modifier</strong> — <code>ArmorOfTheAgesLootModifier</code> renamed to <code>AgeforgedArmorLootModifier</code>.</li>
<li><strong>Template moved</strong> — <code>src/main/templates/</code> → <code>src/main/resources/templates/</code> per WORKFLOW v1.0.0.</li>
<li><strong>build.gradle updated</strong> — template source path adjusted to new location.</li>
</ul>

<h3>Docs</h3>
<ul>
<li><strong>WORKFLOW renamed and aligned</strong> — <code>docs/WORKFLOW.md</code> → <code>docs/WORKFLOW_AGEFORGED_ARMOR_26-1-2.md</code> with v1.0.0 content: naming conventions, typography, CI/CD, Graphify, fork attribution, new directory structure.</li>
<li><strong>Fork attribution added</strong> — README.md and <code>neoforge.mods.toml</code> now credit &quot;Armor of the Ages&quot; by AlxR00T.</li>
</ul>

<h3>Infrastructure</h3>
<ul>
<li><strong>.gitlab-ci.yml created</strong> — CI/CD pipeline for GitHub mirror.</li>
<li><strong>temp/ and libs/ created</strong> — per WORKFLOW structure requirements.</li>
<li><strong>nul file removed</strong> — cleanup of stale artifact.</li>
<li><strong>.gitignore updated</strong> — <code>temp/</code> added.</li>
<li><strong>CHANGELOG.md updated</strong> with comprehensive entry covering beta.24 through beta.28.</li>
</ul>