<h2>v0.0.0-beta.26 — Restore setupAnim before pose copy</h2>

<h3>Fix</h3>
<ul>
<li>Restored <code>model.setupAnim(renderState)</code> before <code>copyPoseFromPlayer</code>. The setupAnim was removed in beta.25, leaving the armor model in default T-pose with no animation. Now base vanilla animation is applied first, then the PlayerModel pose (including Player Animation Core modifications) is copied for rotation/scale.</li>
</ul>