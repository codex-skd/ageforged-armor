<h2>v0.0.0-beta.2 - Fix AvatarRenderer mixin for MC 26.2</h2>

<h3>Fix</h3>
<ul>
<li><strong>Crash on startup</strong>: <code>MixinAvatarRenderer</code> targeted the old <code>renderRightHand</code>/<code>renderLeftHand</code> signature with <code>AbstractClientPlayer</code>. MC 26.2 removed that parameter. Now uses the 5-param version and obtains the player via <code>Minecraft.getInstance().player</code>.</li>
</ul>

<h3>Notes</h3>
<blockquote>This version replaces beta.1 on CurseForge.</blockquote>

<hr>

<p><strong>JAR</strong>: <code>ageforged_armor-26.2-neoforge-0.0.0-beta.2.jar</code></p>
