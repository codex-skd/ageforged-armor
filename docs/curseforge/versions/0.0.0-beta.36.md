<h2>v0.0.0-beta.36 — Fix first-person arm armor position — removed incorrect x/y/z reset</h2>

<h3>Fix</h3>
<ul>
<li><strong>Arm armor rendering at model center</strong>: beta.35 incorrectly reset the arm part&#8217;s x/y/z to 0.0F, placing it at the model root instead of the shoulder position (-5, 2, 0). The arm&#8217;s default position offset is already correct and not modified by <code>setupAnim</code>, so it should not be reset. Restored the default positional offset so the armor arm renders at the correct location relative to the player body.</li>
</ul>