## v0.0.0-beta.18

### Fixed
- Crash on startup: `@WrapOperation` handler was missing the target instance parameter (`HumanoidArmorLayer`) required by MixinExtras for instance method wrapping. This caused a mixin apply failure when loading `HumanoidArmorLayer`, crashing the game during entity renderer registration.
