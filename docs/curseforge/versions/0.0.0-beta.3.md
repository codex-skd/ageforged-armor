<h2>v0.0.0-beta.3 - Fix first-person armor rendering</h2>

<h3>Fix</h3>
<ul>
<li><strong>First-person armor not visible</strong>: <code>MixinAvatarRenderer</code> injectaba al método de 5 parámetros de <code>renderRightHand</code>/<code>renderLeftHand</code>, pero <code>ItemInHandRenderer</code> llama directamente al de 6 parámetros (que recibe <code>Avatar</code>). Cambiado a la versión de 6 parámetros.</li>
</ul>

<h3>Notes</h3>
<blockquote>Ahora la armadura se renderiza correctamente en primera persona.</blockquote>

<hr>

<p><strong>JAR</strong>: <code>ageforged_armor-26.2-neoforge-0.0.0-beta.3.jar</code></p>
