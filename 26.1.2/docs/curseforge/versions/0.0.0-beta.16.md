## v0.0.0-beta.16

### Fixed
- NullPointerException crash when rendering armor on player (entity=null in isSlim)
- EquipmentLayerRenderer no longer attempts to process custom armor items (double render)
- Simplified ForgeHumanoidArmorItem IClientItemExtensions to prevent null entity access
- Added null guard to isSlim() for shadow render passes

### Changed
- Mixin now wraps renderArmorPiece instead of shouldRender (shouldRender is not called in MC 26.1.2)
- Custom armor rendered via @Inject at TAIL after vanilla processing
